/**
 * There are API's handless with file-IO.
 */ 

#ifndef G_TIME_H
#define G_IIME_H

/* get time string[world time].
 *
 * @param str Memory addr of the string.
 * @param mode The time string format.
 * @return
 */
void get_timestamp(char * const str);

#endif
